대표적으로 3가지가 있음
핸들러,로거,루트로거

핸들러는 이벤트가 로그될 "위치" 및 "방법"을 결정.
로거는 범주라고 하는데 관련된 범주로 구성
루트 로거는 부모 로거라고 생각하면 됨

핸들러는 2가지가 있음
콘솔,파일(server.log에 기록) 파일핸들러의 종류는 시간-회전,크기-회전이 있음


기본 log의 위치는 standalone/log domain/log
domain의 서버들에 대한 로그는 domain/server/서버이름/log에 있음
이 위치가 서버에서 문제를 찾을 때 시작하기 좋은 장소임!!!
