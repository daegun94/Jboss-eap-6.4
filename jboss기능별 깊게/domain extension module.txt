Infinispan ->클러스터링을 구현하기위한 네트워크 캐시 컨테이너 생성
세션 복제시 비동기모드,동기모드 옵션이 있음, 또한
repl(전체노드에게복사)dist(부분노드에게복사)옵션 설정 가능


jgroups->클러스터링,고가용성


cmp->?


configadmin->slave node 호스트 컨트롤러가 도메인 컨트롤러에 접근하기위한 secret값을 넣는 곳

connector->웹 서브시스템에서 요청을 받아 들이기 위한 컴포넌트(HTTP,HTTPS,AJP)

ee:java ee (애플리케이션들이 사용하는 컴포넌트 및 서비스의 표준을 정한 것 ?

ejb3->분산,트랜잭션,보안등의 서비스를 제고하는 서버 쪽 컴포넌트

jacorb->JTS 트랜잭션 및 다른 ORB서비스의 CORBA 서비스????

jaxr->java api for xml registries-> 다양한 메타 레지스트리를 다루는 앱들의 표준화된 api

jdr->JBoss Diagnostic Reporter (JDR) Subsystem. ????

jmx-네트워크 등을 감시 및 관리를 위한 도구를 제공하는 api

jpa->jPA는 애플리케이션과 JDBC 사이에서 동작한다. JPA 내부에서 JDBC API를 사용하여 SQL을 호출하여 DB와 통신한다.

jsf->java server face ee기반 앱을 개발하는데 있어 ui를 개발하기 쉽게하는 프레임워크

jsr77->java ee 서버 관리의 표준화를 위한 java사양 요청????????....

logging->로그관련
mail->
messaging->비동기 메시징 시스템(시스템간의)

modcluster->클러스터의 노드들을 자동으로 찾아 로드밸런싱하는방법

naming->jndi관련

pojo->

remoting-> 로컬 및 원격 서비스에 대한 인바운드 및 아웃바운드 연결과 해당 연결에 대한 설정을 구성할 수 있음

sar->서비스 ㅏㅇ카이브..?

security->인증,권한,보안

threads->쓰레드사용

transactions->데베 상태를 변환시키는 하나의 작업단위

web->

webservices->웹 서비스 서브시스템 (들어오는 요청을 어떻게처리)

weld->cdi 구현 ????

<CDI>
Contexts and dependency>



<ORM>
ORM❓
Object-Relational Mapping

객체와 관계형 데이터베이스를 매핑한다는 뜻
ORM 프레임워크는 객체와 테이블을 매핑해 패러다임의 불일치를 개발자 대신 해결해준다. 
객체는 객체대로 생성하고, 데이터베이스는 데이터베이스에 맞도록 설계를 가능하게 해준다.
개발자는 이를 매핑하는 방법만 전달해주면 된다.


<JPA>
자바 ORM에 대한 API 표준 명세이고, 인터페이스의 모음이다. 
따라서 구현체가 없고, 사용하기 위해서는 ORM프레임워크를 선택해야한다.
다양한 프레임워크가 존재하지만 가장 대중적인 것은 하이버 네이트이다.
JPA는 애플리케이션과 JDBC 사이에서 동작한다. JPA 내부에서 JDBC API를 사용하여 SQL을 호출하여 DB와 통신한다.
개발자가 ORM 프레임워크에 저장하면 적절한 INSERT SQL을 생성해 데이터베이스에 저장해주고, 
검색을 하면 적절한 SELECT SQL을 생성해 결과를 객체에 매핑하고 전달해 준다.


<EJB>
분산,트랜잭션,보안등의 서비스를 제공하는 서버 사이드 컴포넌트


<EJB컨테이너>
기업환경의 시스템을 구현하기위한 서버측 컴포넌트 모델
즉 애플리케이션의 업무로직을 가지고 있는 서버 앱.
엔터프라이즈 시스템은 여러 대의 서버를 네트워크상에서 연결하여 시스템을 구성하는 것이 일반적이다.
분산된 시스템에서 동작하는 애플리케이션은 비즈니스 로직뿐만 아니라 네트워크 통신,스레드 관리, 메모리 관리
보안,트랜잭션,확장성등의 기능을 제공할 수 있어야 한다.
또한 대규모로 애플리케이션이 개발되기 때문에 프로그램 이식성,유집수성,개발 생산서을 높이는 일도 매우 중요하다.
ejb는 이러한 요구사항을 만족하게 하기 위한 엔터프라이즈 표준 컴포넌트이다.

ejb 컨테이너는 빈을 분산 컴포넌트로서 동작시키기 위한 트랜잭션이나 보안 등의 각종 서비스를 제공하는 환경.
ejb컨테이너는 ejb를 호출할 때 다른 스레드가 그 ejb 인스턴스에 동시에 접근하지 않도록 보장한다.
즉, 개발자가 빈을 구현할 때 스레드 세이프하게 만들 필요없고 컨테이너가 관리함.
분산 애플리케이션 개발에서 기술적으로 어려운 부분을 컨테이너가 담당해 처리하기때문에 개발이 쉬워짐.
